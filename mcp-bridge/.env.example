# 复制为 .env 后填写。切勿提交 .env 到 Git。
# 至少 24 字符的随机串，可用: python -c "import secrets;print(secrets.token_urlsafe(32))"
MCP_BRIDGE_TOKEN=

# Cloudflare 命名隧道 token（Zero Trust 控制台 -> Networks -> Tunnels 里复制）
TUNNEL_TOKEN=
